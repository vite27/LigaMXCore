@model LigaMXCore.Models.Pais

@{
    ViewData["Title"] = "Editar País";
    Layout = "_Layout";
}

<h1>Editar País</h1>

<form asp-action="Edit" method="post">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <input type="hidden" asp-for="PaisId" />

    <div class="form-group">
        <label asp-for="PaisNombre" class="control-label">Nombre del País</label>
        <input asp-for="PaisNombre" class="form-control" />
        <span asp-validation-for="PaisNombre" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Guardar</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
